#include <iostream>

using namespace std;

int main()
{
    // this is a comment - used only to instruct what the program is doing
    /*
        Everything between these lines is considered a comment 
    */
    int slices;
    cout << "How many pieces o pizza?: ";
    cin >> slices;
    cout << "You have " << slices << " slices of pizza." << endl;
    return 0;
}